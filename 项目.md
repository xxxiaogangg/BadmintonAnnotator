# 羽毛球技战术分析与标注工具 项目说明

## 1. 项目概览
- 目标：对羽毛球比赛视频进行高效标注与审阅，输出结构化 JSON 结果。
- 技术栈：Python 3.9+、PyQt6、OpenCV（opencv-python）。
- 支持平台：macOS / Windows / Linux。
- 维护要求：三端（macOS/Windows/Linux）同步维护与验证，避免依赖单一平台特性。

## 2. 快速开始
```bash
# 建议使用虚拟环境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

pip install -r requirements.txt
python main.py
```

## 3. 目录结构速览
- `main.py`：主窗口与绝大多数业务逻辑（播放、事件树、审阅、快捷键等）。
- `core/video_worker.py`：后台线程解码与播放逻辑。
- `core/data_model.py`：标注 JSON 初始化结构。
- `widgets/drawing_label.py`：画框/标点画布、技术动作选择对话框、枚举表。
- `widgets/match_setup_dialog.py`：比赛信息输入对话框。
- `widgets/serve_player_dialog.py`：发球方 / 得分方选择对话框。
- `mixins/review_mixin.py`：审阅统计与跳转逻辑（从 `main.py` 拆出）。
- `mixins/event_tree_mixin.py`：事件树构建/选中/编辑逻辑（从 `main.py` 拆出）。
- `scripts/`：训练数据准备、视频转 GIF 等辅助脚本。
- `标注指南/`、`Need_to_know.md`：标注规范与注意事项。

## 4. 数据模型（JSON）
标注文件默认与视频同名，后缀 `.json`，结构示例：
```json
{
  "schema_version": 1,
  "video_info": {
    "filename": "xxx.mp4",
    "path": "/path/to/xxx.mp4",
    "resolution": [1920, 1080],
    "fps": 30,
    "total_frames": 12345
  },
  "match_info": {
    "event_name": "未命名赛事",
    "event_level": "未知",
    "match_stage": "未知",
    "player_a": "球员A",
    "player_b": "球员B",
    "set_scores": [],
    "current_set_score": [0, 0]
  },
  "events": [
    {
      "event_id": "evt_xxx",
      "type": "RALLY_START",
      "frame": 123,
      "details": {
        "serving_player": "球员A",
        "score_at_start": [0, 0],
        "hand": "待定",
        "major": "待定",
        "minor": "待定",
        "view_desc": "视角正常"
      }
    }
  ],
  "frame_annotations": {
    "123": [
      { "id": "box_xxx", "type": "box", "label": "player", "bbox": [x, y, w, h] },
      { "id": "point_xxx", "type": "point", "label": "ball", "coords": [x, y] }
    ]
  }
}
```
事件类型：`SET_START` / `SET_END` / `RALLY_START` / `RALLY_END` / `SHOT`。

## 5. 核心流程
1. **启动与加载**
   - `start_session()` 会尝试自动加载同名标注文件或自动备份。
   - 无标注时弹出比赛信息对话框初始化数据。
2. **播放与跳转**
   - `VideoWorker` 在后台线程解码；主线程负责渲染。
   - 帧跳转通过 `seek_video()` 与滑条完成。
3. **事件标注**
   - Set/Rally/Shot 事件由快捷键或按钮触发。
   - 回合逻辑通过时间轴归属判断（避免落到错误回合）。
4. **审阅与补全**
   - 右侧“击球事件审阅”提供待定/不适用/视角异常/击球缺帧/非常规动作筛选。
   - 选中事件后按 Enter 编辑动作细节。
5. **击球循环**
   - 右侧开关“击球循环”用于在两次击球之间循环播放。
   - 循环以事件树当前选中事件为基准。
   - 循环区间长度仍基于相邻击球，但播放窗口会以当前选中帧为中点。
   - 循环起止会额外向前/向后各扩展 0.5 秒。
6. **画框/标点**
   - 画布 `DrawingLabel` 支持框选球员、标注球、拖拽/缩放与删除。

## 6. 关键枚举与配置入口
位于 `widgets/drawing_label.py`：
- `TECHNIQUES`：技术动作大类/小类。
- `NOTSUIT`：不适用原因（含“凑击球”“非常规动作”）。
- `VIEWDESCP`：视角描述。
- `HAND_TYPES`：适用/不适用/待定。

## 7. 构建与发布
```bash
pip install -r requirements.txt -r requirements-build.txt
python build.py
```
`build.py` 使用 PyInstaller 打包，输出在 `release/`。
macOS 会强制使用 onedir `.app` 方式构建，并输出 zip 分发包；可用
`MACOS_TARGET_ARCH=arm64`、`MACOS_TARGET_ARCH=x86_64` 或
`MACOS_TARGET_ARCH=universal2` 指定目标架构。公开分发仍建议使用
Developer ID 签名并公证。发布时以 `python build.py` 为准，不直接复用
本地生成过的 `.spec` 文件。

## 8. 贡献与扩展指南
常见扩展点：
- 新增技术动作：更新 `TECHNIQUES` 或 `NOTSUIT`。
- 新增审阅筛选项：更新 `review_index_map`、`update_review_stats()` 与导航函数。
- 新增事件类型：更新事件树渲染、比分回放与编辑逻辑。
- 事件详情编辑时优先用事件树的局部更新（`_update_event_item_text`），避免全量 `refresh_all_ui()`。
- 回合内击球员重评后可用 `_update_event_items` 批量刷新对应行，避免全量刷新。
- 无标注初始化时可用 `_reset_event_tree_view` 快速清空事件树与缓存。
- 加载标注后若事件为空，优先 `_reset_event_tree_view`，避免无效全量刷新。
- 新增事件若位于时间线末尾，可用 `_insert_event_item` 追加到事件树，避免全量刷新。
- 未先建局时仍可追加 `RALLY_START`/`SET_END`，事件树会以顶层行显示并保底写入文案/比分。
- 比分重算后建议调用 `_refresh_score_related_items` 更新 `RALLY_START`/`SET_END` 显示，避免全量刷新。

## 9. 排错与约定
- 标注规范参见 `Need_to_know.md` 与 `标注指南/`。
- 日志目前使用 `print()`，调试时可改为 `logging` 统一管理。
- 线程约定：`VideoWorker` 内部使用互斥锁保护播放/跳转状态，避免在子线程调用 `QApplication.processEvents()`。

## 10. TODO / 进度
### 已完成
- [x] 事件树刷新逻辑统一为 `refresh_all_ui()`（避免重复函数维护）。
- [x] 事件索引映射 `event_id -> event`，降低查找开销。
- [x] `_insert_event_item` 提前登记 `event_items`，避免增量插入时出现空白行/文本缺失。
- [x] `drawing_label.py` 坐标转换边界检查（避免除零）。
- [x] `VideoWorker` 播放循环增加最小 sleep，避免 CPU 占用过高。
- [x] `VideoWorker` 播放结束信号 `playback_finished`，UI 自动回退暂停状态。
- [x] 帧标注读取缓存（`_frame_annotations_cache`），减少播放时频繁字典/字符串转换。
- [x] 启动/加载/保存错误提示细化（QMessageBox + 日志/状态栏提示）。
- [x] 步进间隔/自动保存时间抽为常量，并以视频 FPS 为基准。
- [x] 事件树“非末尾插入”改为局部重建（减少全量刷新）。
- [x] 清理未使用的备份函数（未发现 `_create_menu_bar_bak()` 等残留）。
- [x] SET_START 增量插入计数修复（首局显示为第 1 局），插入后列宽自适应。

### 进行中
- 无

### 待办
- 无
