# 代码优化建议报告

## 🔴 高优先级问题

### 1. **代码重复：`refresh_event_tree()` 和 `refresh_all_ui()` 几乎完全重复**
   - **位置**: 第 1011-1227 行
   - **问题**: 两个函数有 90% 的重复代码，只有最后的滚动逻辑不同
   - **影响**: 代码维护困难，修改逻辑需要在两个地方同步
   - **建议**: 
     - 合并为一个函数 `refresh_event_tree(scroll_to_event_id=None, scroll_to_bottom=False)`
     - 删除 `refresh_event_tree()`，所有调用改为 `refresh_all_ui()`

### 2. **数据一致性问题：添加事件时未重新计算比分**
   - **位置**: `add_event()` 方法（第 1003 行）
   - **问题**: 删除事件时会调用 `recalculate_scores()`，但添加/修改事件时不会
   - **影响**: 如果用户手动编辑了事件，比分可能不一致
   - **建议**: 
     - 在 `add_event()` 中调用 `recalculate_scores()` 或仅对影响比分的事件类型调用
     - 或者在 `add_rally_end_event()` 中确保比分更新逻辑正确

### 3. **性能问题：播放时频繁加载标注数据**
   - **位置**: `update_frame()` 方法（第 665-682 行）
   - **问题**: 播放时每次都要从字典中查找并加载标注数据
   - **影响**: 可能影响播放流畅度，特别是标注数据量大时
   - **建议**: 
     - 考虑使用缓存机制
     - 或者优化数据结构，使用按帧索引的列表而不是字典

### 4. **边界检查缺失：坐标转换时可能除零**
   - **位置**: `drawing_label.py` 中的坐标转换函数
   - **问题**: `ui_coord_to_video_coord_rect()` 等方法中，`self.width()` 或 `self.original_video_width` 可能为 0
   - **影响**: 可能导致除零错误或坐标错误
   - **建议**: 添加边界检查，确保分母不为 0

## 🟡 中优先级问题

### 5. **错误处理不完善**
   - **位置**: 多处文件操作和视频操作
   - **问题**: 
     - `save_annotations()` 和 `load_annotations()` 的异常处理过于简单
     - `start_session()` 中视频打开失败时没有用户友好的提示
     - `VideoWorker` 中错误处理可以更详细
   - **建议**: 
     - 添加更具体的异常类型捕获
     - 使用 `QMessageBox` 显示错误信息给用户
     - 记录详细的错误日志

### 6. **事件查找效率低**
   - **位置**: 多处使用 `next((e for e in events if ...), None)` 或 `events.index(event)`
   - **问题**: 
     - `events.index()` 在列表中查找是 O(n)
     - 多次遍历同一列表
   - **建议**: 
     - 使用字典建立 `event_id -> event` 的映射，O(1) 查找
     - 或者使用 `enumerate()` 在单次遍历中同时获取索引和事件

### 7. **线程安全问题**
   - **位置**: `VideoWorker` 的 `seek()` 方法
   - **问题**: `frame_to_seek` 的读写没有锁保护（虽然在这个场景下可能不会出问题，但不是最佳实践）
   - **建议**: 
     - 使用 `QMutex` 或 `QWaitCondition` 保护共享状态
     - 或者使用信号队列机制

### 8. **资源管理：视频捕获对象未正确释放**
   - **位置**: `start_session()` 方法（第 558 行）
   - **问题**: 在打开新视频前，如果旧视频的 `cv2.VideoCapture` 对象未正确释放，可能造成资源泄漏
   - **建议**: 确保在 `VideoWorker` 的 `stop()` 方法中正确释放资源

### 9. **代码组织：函数过长**
   - **位置**: 
     - `refresh_all_ui()` 约 120 行
     - `start_session()` 约 90 行
     - `add_shot_event()` 约 70 行
   - **建议**: 
     - 将事件树构建逻辑提取为独立方法
     - 将节点文本填充逻辑提取为独立方法
     - 将自动加载逻辑提取为独立方法

### 10. **魔法数字和硬编码值**
   - **位置**: 
     - 第 164 行：`self.step_intervals = {"1 帧": 1, "10 帧": 10, "1 秒": 30, "5 秒": 150}` - 30fps 硬编码
     - 第 660 行：`self.autosave_timer.start(60000)` - 60 秒硬编码
   - **建议**: 
     - 使用常量定义这些值
     - 步进间隔应该基于实际视频 FPS，而不是硬编码 30

## 🟢 低优先级优化

### 11. **UI 刷新优化**
   - **问题**: 某些操作可能触发多次 UI 刷新
   - **建议**: 使用 `QTimer.singleShot()` 延迟刷新，合并多次刷新请求

### 12. **快捷键冲突检查**
   - **问题**: `eventFilter` 和菜单快捷键可能冲突
   - **建议**: 统一管理快捷键，避免重复定义

### 13. **代码注释和文档**
   - **问题**: 部分复杂逻辑缺少注释
   - **建议**: 为复杂函数添加详细的文档字符串

### 14. **类型提示**
   - **问题**: 缺少类型提示，影响代码可读性和 IDE 支持
   - **建议**: 添加类型提示（Python 3.5+）

### 15. **未使用的代码**
   - **位置**: 
     - `_create_menu_bar_bak()` 方法（第 69 行）
     - `_create_shortcuts_bak()` 方法（第 391 行）
   - **建议**: 删除未使用的代码

## 📊 VideoWorker 特定优化

### 16. **播放循环中的时间计算**
   - **位置**: `VideoWorker.run()` 方法（第 86-90 行）
   - **问题**: `sleep_time` 可能为负数，导致立即处理下一帧，可能造成 CPU 占用过高
   - **建议**: 确保 `sleep_time` 至少为某个最小值（如 1ms）

### 17. **视频结束检测**
   - **位置**: `VideoWorker.run()` 方法（第 78-81 行）
   - **问题**: 视频结束时只是暂停，没有通知主窗口
   - **建议**: 发送信号通知主窗口视频已结束

### 18. **帧号获取的准确性**
   - **位置**: `VideoWorker.run()` 方法（第 65、83 行）
   - **问题**: `cap.get(cv2.CAP_PROP_POS_FRAMES) - 1` 可能不准确
   - **建议**: 使用 `frame_to_seek` 的值作为当前帧号，更可靠

## 🎯 建议的优化顺序

1. **立即修复**: #1 (代码重复), #2 (数据一致性), #4 (除零检查)
2. **短期优化**: #5 (错误处理), #6 (查找效率), #9 (代码组织)
3. **中期优化**: #3 (性能优化), #7 (线程安全), #8 (资源管理)
4. **长期改进**: #10-18 (代码质量提升)

## 💡 额外建议

- **单元测试**: 添加单元测试，特别是对数据模型和事件处理逻辑
- **性能分析**: 使用 `cProfile` 分析性能瓶颈
- **日志系统**: 实现统一的日志系统，替代 `print()` 语句
- **配置管理**: 将硬编码的配置项提取到配置文件

