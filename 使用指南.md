# 羽毛球技战术分析工具 (Badminton Annotator) v2.2.2 使用指南

欢迎使用 **Badminton Annotator**！这是一款专为羽毛球项目设计的深度技战术分析软件，支持高清视频流畅播放、多维度赛事数据标注以及对象轨迹追踪。

本文档将帮助您快速掌握软件的各项功能。

---

## 1. 快速开始 (Quick Start)

### 1.1 启动软件
请确保您已配置好 Python 环境并安装了依赖项（详见 `README.md`）。在终端运行：
```bash
python main.py
```

### 1.2 加载视频
1.  点击菜单栏 **文件 (File) -> 打开视频 (Open Video)**，或使用快捷键 `Ctrl+O`。
2.  选择您的比赛视频文件（支持 MP4, AVI 等常见格式）。
3.  **自动恢复**：如果您上次异常退出或关闭了软件，再次打开时，软件会询问是否恢复播放上次的视频。
4.  **比赛设置**：首次打开新视频时，会弹出比赛信息设置对话框，请输入**比赛名称**（如 "2024苏迪曼杯决赛"）和**运动员姓名**（默认为 球员A / 球员B）。

---

## 2. 界面概览

软件主界面主要分为三个区域：

### 2.1 左侧：视频播放区
*   **主画面**：显示视频内容。支持鼠标操作进行画框（球员）和打点（羽毛球）。
*   **控制栏**：
    *   **进度条**：拖动以快速定位。
    *   **播放控制**：播放/暂停 (`空格键`)，帧步进 (`←`/`→`)。
    *   **倍速**：支持 `0.5x` (慢放) 到 `4.0x` (快进)。
    *   **步进设置**：下拉选择 `<<` / `>>` 按钮的跳转跨度（如 10帧、1秒、5秒）。

### 2.2 右侧顶部：功能切换区
通过下拉菜单切换右侧面板的工作模式：
*   **球与运动员**：用于对象标注（画框、画点）。
*   **标注击球事件**：显示比分板和常用标注按钮。
*   **击球事件审阅**：专门用于检查和补全“待定”或“不适用”的事件。
*   **击球事件审阅（横屏）**： 切换到上下分屏模式，将事件列表横向置于底部，适合宽屏显示器。

### 2.3 右侧底部：事件浏览器 (Event Tree)
*   以树状结构（局 -> 回合 -> 击球）显示整场比赛的事件流。
*   **双击**任意事件可编辑其详细信息（技术动作、得分原因等）。

---

## 3. 核心操作流程

### 流程一：比赛事件标注 (Event Tagging)
这是本软件最核心的功能，建议使用键盘快捷键以提高效率。推荐的工作流如下：

1.  **开始一局**:
    *   按 `S` 键（或点击“局开始”）。
    *   比分会自动重置为 0-0。
2.  **开始回合**:
    *   当发球动作发生时，按 `R` 键。
    *   如果是0-0开局，需要选择发球方；后续回合会自动根据上一分得主判断发球方。
3.  **记录击球**:
    *   每当球员击球时，按 `I` 键。
    *   系统会自动记录当前帧，并创建一个“待定”的击球事件。
    *   *提示：此时无需纠结技术细节，先把点打上，后续再审阅。*
4.  **回合结束**:
    *   当球落地或死球时，按 `E` 键。
    *   系统会弹出对话框询问**得分方**（球员A / 球员B），选择后比分自动更新。
5.  **局结束**:
    *   一局打完后，按 `Shift + S` 存档当前局比分。

### 流程二：对象标注 (Object Annotation)
用于标记每一帧中的运动员位置和羽毛球位置，适用于训练计算机视觉模型。

1.  **切换模式**：
    *   `Q` 键：**选择/编辑模式**（默认）。可选中已画的框或点，进行拖拽、缩放或删除。
    *   `W` 键：**框选模式**。鼠标拖拽画矩形框，用于标记运动员。
    *   `A` 键：**标记点模式**。鼠标点击画面，用于标记羽毛球（小黄点）。
2.  **删除对象**:
    *   选中对象（框变红或点变红），按 `Delete` 删除。

### 流程三：智能审阅与修整
标注完比赛后，通常会有大量“待定”的属性需要补充。

1.  切换右侧下拉框到 **“击球事件审阅”**。
2.  **筛选**：选择“待定”过滤器。
3.  **导航**：点击“下一个”，软件会自动跳转到第一个未标记技术动作的击球事件，并暂停视频。
4.  **编辑**：按 `Enter` 键打开详情对话框。
    *   使用键盘 `←`/`→` 切换列（手别、大类、小类）。
    *   使用 `↑`/`↓` 选择具体技术（如：正手 -> 杀球 -> 正手杀球）。
    *   按 `Enter` 确认。
5.  **微调帧号**：
    *   如果发现击球点打早了或晚了，无需删除重来。
    *   选中事件，按 `Shift + ←` （前移1帧）或 `Shift + →` （后移1帧）。

---

## 4. 快捷键大全 (Cheat Sheet)

| 功能板块 | 快捷键 | 作用描述 |
| :--- | :--- | :--- |
| **播放控制** | | |
| 播放 / 暂停 | `空格 (Space)` | |
| 上一帧 / 下一帧 | `←` / `→` | 精确移动 1 帧 |
| 快退 / 快进 | `Ctrl` + `←` / `→` | 按照设定的步长（如1秒）跳转 |
| **标注 - 比赛流程** | | |
| 局开始 | `S` | Start Set |
| 局结束 | `Shift` + `S` | End Set |
| 回合开始 | `R` | Rally Start |
| 回合结束 | `E` | Rally End (需选择得分方) |
| 添加击球 | `I` | Insert Shot (打点) |
| **标注 - 对象工具** | | |
| 选择模式 | `Q` | 选中、拖拽、调整对象 |
| 框选模式 | `W` | 画矩形框 (运动员) |
| 标点模式 | `A` | 画点 (羽毛球) |
| **事件审阅与编辑** | | |
| 选中上一个事件 | `↑` | 在列表中向上移动 |
| 选中下一个事件 | `↓` | 在列表中向下移动 |
| **微调事件帧号** | `Shift` + `←` / `→` | 将当前事件的时间点前/后移 1 帧 |
| 编辑事件详情 | `Enter` | 打开属性选择对话框 |
| 删除选中项 | `Delete` / `Backspace` | 删除事件或画布对象 |
| **文件操作** | | |
| 保存标注 | `Ctrl` + `S` | 保存为 JSON 文件 |
| 加载标注 | `Ctrl` + `O` | 加载 JSON 文件 |

---

## 5. 常见问题 (FAQ)

**Q: 只有“回合开始”没有“回合结束”怎么办？**
A: `R` (开始) 和 `E` (结束) 必须成对出现。如果您忘记按 `R`，软件会禁止您直接按 `E`。请先回退视频到发球那一刻补按 `R`。

**Q: 击球点帧号对不准怎么办？**
A: 不需要删除重建。在事件列表中选中该事件，按住 `Shift` 键并连续按 `←` 或 `→` 即可逐帧微调，非常方便。

**Q: 软件自动备份的文件在哪里？**
A: 自动备份文件通常保存在视频同目录下，文件名后缀为 `.autosave.json`。如果程序崩溃，重新打开视频时会提示您恢复。

**Q: 如何切换到横屏审阅模式？**
A: 这是 **v2.2.2** 的新功能。在右侧顶部的下拉菜单中选择 **“击球事件审阅（横屏）”** 即可。

---
*文档生成日期: 2026-01-27*
